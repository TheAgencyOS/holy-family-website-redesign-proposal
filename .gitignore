# Secrets — never commit
.env
.env.*
!.env.example

# Dependencies
node_modules/

# macOS
.DS_Store

# Build / cache
.vercel
.cache/
dist/
build/
public/

# Heavy QA/audit artifacts (77MB, not needed to run the site)
audit/

# Backup HTML files
*.backup.html
*.backup.pdf
index-old-wrapper.html
index.v2.backup.html
index.v5.backup.html
index.OLD.html
index.PRINT-ATTEMPT.html

# Dev-only vendored tooling (loaded for local accessibility audits)
assets/axe.min.js

# Proposal PDF is SERVED from the live site as the official download target.
# HFU-Proposal.pdf is intentionally committed — see site/index.html sidebar button.
# Only ignore the backup.
HFU-Proposal.backup.pdf

# Test PDFs — debug artifacts, not referenced
test-*.pdf

# Editor
*.swp
*.swo
.idea/
.vscode/

# Logs
*.log
npm-debug.log*
